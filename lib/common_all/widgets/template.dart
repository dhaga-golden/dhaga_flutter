import 'package:flutter/material.dart';


class TemplateClass extends StatelessWidget {
  const TemplateClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red[100],);
  }
}
